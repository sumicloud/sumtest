pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
            steps {
                bash ''''
                   #!/bin/bash
                   echo "updating firewall rule"
            }
        }
    }
}
